# Functions in python are objects that can be passed around. So, we can pass one function as parameter in other function as well

# function that takes arguments
def argumentFunc(arg1, arg2):
  pring(arg1, " ", arg2)
  
# function to return something
def findCube(x)
  return x*x*x
  
# Calling a function
argumentFunc(10,20)             #10 20
print(argumentFunc(10,20))      #10 20 and in new line none as it returns nothing

pring(findCube(2))              #8

  
